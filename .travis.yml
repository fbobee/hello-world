language: cpp

jobs:
  inlcude:
    #- name: "cpp"
    #  script: ./configure && make && make test
    #- name: "another cpp"
    #  script: ./configure && make && make test
    - name: "alma"
      stage: "blah"
      script: echo alma
    - name: "korte"
      stage: "blahblah"
      script: echo korte
